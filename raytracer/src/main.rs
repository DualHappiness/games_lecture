use raytracer::*;
fn main() {
    println!("Hello, world!");
    sphere::Sphere::new(
        nalgebra::zero(),
        0f32,
        nalgebra::zero(),
        nalgebra::zero(),
        0f32,
        0f32,
    );
}
